workflow: GitHubFlow/v1
branches:
  main:
    label: beta
    deployment-mode: ContinuousDeployment
  hotfix:
    regex: ^hotfix(es)?[/-]
    source-branches:
      - main
    label: useBranchName
    deployment-mode: ContinuousDeployment